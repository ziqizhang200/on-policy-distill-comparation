# AGENTS.md

## 1. 项目基本信息

本项目为本科毕业设计项目。

项目名称:**基于在线策略蒸馏的大语言模型后训练方法研究**

Git 仓库：

`https://github.com/ziqizhang200/on-policy-distill-comparation.git`

项目最终成果包括：

- 可运行、可复现的实验代码；
- 完整实验结果与分析；
- 项目文档；
- 哈尔滨工业大学本科毕业论文（设计）。

本项目中的交流、项目文档、实验分析、报告和论文均使用中文。

代码、类名、函数名、模型名、数据集名及通用技术术语可以使用英文。

---

## 2. 总体工作原则

本项目所有工作优先保证：

- 结构清晰；
- 接口明确；
- 代码可测试；
- 实验可复现；
- 文档与代码同步；
- 文献真实可靠；
- 论文内容能够由真实代码、实验和文献支撑。

禁止为了快速实现功能而无计划地堆积代码。

对于核心功能，应遵循：

`需求分析 → 功能定义 → 接口设计 → 测试设计 → 实现 → 单元测试 → 实验验证 → 文档更新 → Git Commit`

---

## 3. 主 Agent 与 Sub-Agent 分工

### 主 Agent

主 Agent 负责：

- 研究思路；
- 项目架构；
- 模块划分；
- 接口定义；
- 算法分析；
- 实验设计；
- 论文大纲；
- 实验结果分析；
- 文献筛选；
- 代码审查；
- 最终技术决策。

涉及大纲、接口、模块职责、实验方案等问题时，应先由主 Agent 完成设计。

在当前环境支持委派的前提下，主 Agent 负责设计、任务拆分、结果审查和最终决策，不直接执行本应交给 Sub-Agent 的命令。Git / Bash / PowerShell / Terminal 命令（包括状态检查、文件枚举与读取、初始化、拉取、暂存、提交、推送）、测试及脚本执行原则上统一交给 Sub-Agent 或代码执行 Agent，不得仅因任务简单或操作便利而由主 Agent 直接执行。主 Agent 不应只委派检查，再亲自执行后续命令。仅在 Sub-Agent 不可用、工具限制或更高优先级指令要求主 Agent 执行时例外；发生例外时，应向用户说明原因。

### Sub-Agent

以下任务应当交给 Sub-Agent 或代码执行 Agent：

- Git / Bash / Terminal 指令执行；
- 大段代码生成；
- 批量代码修改；
- 自动化测试；
- 批量修复；
- 实验脚本执行；
- 重复性工程任务。

在不需要并行的纯命令任务中，可将任务整体交给一个执行 Sub-Agent，不为凑并行创建空任务。

若运行环境允许指定模型，优先使用：

`GPT-5.6 Luna`

并使用最高可用推理强度（当前为 `max`）。

如果环境不支持指定模型，则使用当前可用的代码执行 Agent。

---

## 4. 代码生成前必须先设计

新增核心模块或进行较大修改前，必须先明确：

1. 模块解决什么问题；
2. 模块负责什么、不负责什么；
3. 输入与输出；
4. 公共接口；
5. Tensor Shape / dtype 等关键数据格式；
6. 与现有模块的依赖关系；
7. 测试方案。

在接口和职责尚未确定前，不应直接生成大段代码。

对于简单 Bug 修复或局部小修改，可以适当简化流程。

---

## 5. 测试要求

每个核心模块应具有对应的单元测试。

推荐结构：

```text
src/
  models/
  distillation/
  trainers/

tests/
  models/
  distillation/
  trainers/
```

新增或修改核心功能时，应同步新增或更新测试。

测试根据模块实际情况至少考虑：

- 正常输入；
- 边界输入；
- 异常输入；
- 输出 Shape；
- dtype；
- 数值正确性；
- 梯度是否可以正常传播；
- NaN / Inf；
- CPU / CUDA 兼容性。

不得仅因为代码“可以运行”就认为功能已经完成。

---

## 6. 项目文档维护

项目根目录长期维护：

```text
README.md
STAGE.md
Interface.md
```

### README.md

README 面向项目使用者，至少维护：

- 项目简介；
- 研究目标；
- 项目结构；
- 环境安装；
- 数据准备；
- 训练方式；
- 评估方式；
- 实验复现方式；
- 当前实现状态。

重要使用方式发生变化时，应同步更新 README。

### STAGE.md

`STAGE.md` 用于维护项目长期状态和项目记忆。

至少包含：

```markdown
# STAGE

## 当前进展
## 正在进行
## 后续目标
## 重要设计决策
## 已知问题
## 实验进度
## 已确认注意事项
```

以下情况应更新 `STAGE.md`：

- 完成核心模块；
- 修改项目架构；
- 修改重要接口；
- 完成重要实验；
- 得出会影响后续工作的结论；
- 发现重要问题；
- 修改后续开发计划。

不要把大量运行日志直接堆入 `STAGE.md`。

### Interface.md

`Interface.md` 用于维护项目中的公共和跨模块接口，包括但不限于：

- Model；
- Dataset；
- Trainer；
- Distillation Strategy；
- Loss；
- Evaluator；
- CLI；
- 配置文件；
- Tensor 输入输出；
- Checkpoint；
- 实验结果格式。

修改公共接口时必须同步更新 `Interface.md`。

---

## 7. Git 规范

`.gitignore` 仅负责维护版本控制排除规则。

以下内容原则上不得提交：

- 数据集；
- 模型权重；
- Checkpoint；
- Cache；
- 临时文件；
- 日志；
- IDE 配置；
- `.env`；
- API Key；
- Token；
- 密码；
- 其他私密信息。

每完成一个具有独立意义的修改，应创建独立 Git Commit。

推荐 Commit 类型：

```text
feat: 新功能
fix: Bug 修复
test: 测试
docs: 文档
refactor: 重构
exp: 实验
perf: 性能优化
chore: 工程配置
```

Commit Message 必须能够说明本次修改的实际内容。

避免使用：

```text
update
modify
fix
test
123
```

等无意义信息。

不要将多个无关修改混入同一个 Commit。

---

## 8. 实验与可复现性

正式实验应尽可能通过配置文件管理，而不是在源码中硬编码参数。

推荐：

```text
configs/
experiments/
scripts/
```

任何准备进入毕业论文的正式实验，都应能够追溯到至少以下信息：

- Git Commit Hash；
- 实验配置；
- 随机种子；
- 数据集及版本；
- Teacher 模型；
- Student 模型；
- 模型版本；
- 关键超参数；
- 主要依赖版本；
- GPU / CUDA / PyTorch 环境；
- Evaluation Metric；
- 原始实验结果。

推荐每个正式实验至少保存：

```text
experiments/<exp-name>/
  README.md
  config.yaml
  metrics.json
```

必要时增加：

```text
command.txt
environment.txt
logs/
plots/
```

论文中的图表应尽可能由实验结果通过脚本自动生成。

禁止：

- 手工修改实验结果；
- 无记录地修改实验参数；
- 混用不同 Commit 下无法确认来源的实验结果；
- 将尚未执行的实验结果写入论文；
- 虚构实验结果。

---

## 9. 实验比较原则

进行不同蒸馏方法比较时，应尽可能控制变量。

重点保持一致：

- Student Model；
- Dataset；
- Evaluation Set；
- Training Budget；
- Optimizer；
- Learning Rate；
- Batch Size；
- Training Steps；
- Generation Parameters；
- Evaluation Metric；
- 硬件环境。

如果某些变量必须不同，应在实验记录和论文中明确说明原因。

---

## 10. 文献管理

正式参考文献统一使用 BibTeX 管理。

推荐统一维护：

```text
/Papers/references.bib
```

除模板明确要求外，不应建立多个独立 `.bib` 文件。

任何加入正式 `.bib` 的参考文献都必须经过实际检索验证。

不得仅根据模型记忆生成参考文献。

优先通过以下来源确认：

- DOI；
- ACM Digital Library；
- IEEE Xplore；
- ACL Anthology；
- arXiv；
- Springer；
- Elsevier；
- DBLP；
- 官方会议页面；
- 作者或机构官方网站。

至少验证：

- Title；
- Authors；
- Year；
- Venue；
- DOI 或 arXiv ID（若存在）。

禁止虚构：

- 作者；
- 标题；
- DOI；
- 年份；
- Conference；
- Journal；
- 页码；
- Volume / Issue。

无法确认的信息应明确说明，不能猜测。

引用算法或模型时，应优先引用其原始论文。

---

## 11. 论文与报告规范

本项目所有正式报告和毕业论文均使用中文。

格式按照：

**哈尔滨工业大学本科毕业论文（设计）**

要求执行。

相关文件位于：

```text
/Papers/examples
/Papers/templates
/Papers/output
```

### Papers/examples

用于参考：

- 段落格式；
- 标题层级；
- 图表格式；
- 引用格式；
- 章节组织方式。

仅参考格式，不需要参考示例论文的研究内容。

### Papers/templates

第一次开始撰写某份正式文档时，应从：

```text
/Papers/templates
```

中选择对应模板。

模板目录原则上视为只读，不直接长期修改模板源文件。

### Papers/output

实际生成的论文和报告必须保存至：

```text
/Papers/output
```

第一次从模板创建后，后续修改均基于 `/Papers/output` 中的文件。

不要重复从模板重新生成并覆盖已有内容。

如一般写作习惯与 HIT 模板冲突，以 HIT 模板和 `/Papers/examples` 中能够确认的格式为准。

---

## 12. 论文内容真实性

论文中的内容必须能够由实际研究过程支撑。

禁止：

- 虚构实验；
- 虚构数据；
- 虚构结果；
- 虚构模型配置；
- 虚构硬件环境；
- 虚构文献；
- 将推测写成实验结论。

如果实验尚未完成，应明确标记为：

`待实验验证`

而不是提前填写结果。

研究讨论中应区分：

- 已知事实；
- 文献结论；
- 实验结果；
- 推测；
- 待验证假设。

---

## 13. 修改已有项目时的原则

修改代码前，应先阅读相关已有实现、测试和接口文档。

优先进行最小必要修改。

除非已经明确存在架构问题，否则不要为了一个小功能：

- 重写整个模块；
- 大规模改名；
- 大规模移动目录；
- 修改无关代码；
- 重构整个项目。

重要重构前应明确：

- 为什么需要重构；
- 影响哪些接口；
- 是否影响已有实验；
- 是否影响已有配置和 Checkpoint。

---

## 14. 任务完成标准

核心任务不能仅以“代码写完”为完成标准。

完成一个核心任务后，应根据实际情况检查：

```text
功能实现
+
单元测试
+
实际运行验证
+
Interface.md
+
STAGE.md
+
README.md
+
必要实验
+
Git Commit
```

并只更新真正受到影响的文件。

---

## 15. 禁止虚构执行状态

Agent 不得声称未实际完成的操作已经完成。

禁止虚构：

- 已执行某条命令；
- 测试已经通过；
- 实验已经运行；
- Git Commit 已创建；
- 文件已经修改；
- 文献已经验证；
- 论文格式已经检查；
- 某个结果已经复现。

涉及 Git、Shell、测试、实验等操作时，应尽可能实际执行并根据真实输出继续工作。

---

## 16. 项目最终目标

本项目最终目标不是单纯完成代码，而是形成一套：

- 研究问题明确；
- 文献基础可信；
- 方法实现完整；
- 接口设计合理；
- 单元测试充分；
- 实验比较公平；
- 实验过程可复现；
- 实验结果可信；
- 论文写作规范；

的哈尔滨工业大学本科毕业设计成果。

所有代码、实验、文档和论文工作都应服务于这一目标。
